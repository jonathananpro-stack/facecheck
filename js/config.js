// Cấu hình hệ thống Mắt thần
export const CONFIG = {
    // Ngưỡng tin cậy (0-1), 0.5 là mức cân bằng
    minConfidence: 0.5,
    // Độ nhạy nhận diện cơ thể (Pose)
    poseSensitivity: 0.7,
    // Tên database ẩn danh
    dbName: "SEC_CORE_VECTOR_DB",
    // Chế độ bảo mật (mã hóa vector)
    encryptionEnabled: true
};
